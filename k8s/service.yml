apiVersion: v1
kind: Service
metadata:
  name: mkloiu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mkloiu"
    port: 8080
  selector:
    app: mkloiu
