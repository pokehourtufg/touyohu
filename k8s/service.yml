apiVersion: v1
kind: Service
metadata:
  name: sdghv
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sdghv"
    port: 8080
  selector:
    app: sdghv
