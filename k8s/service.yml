apiVersion: v1
kind: Service
metadata:
  name: ouvz
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ouvz"
    port: 8080
  selector:
    app: ouvz
