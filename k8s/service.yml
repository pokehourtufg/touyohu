apiVersion: v1
kind: Service
metadata:
  name: coern
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "coern"
    port: 8080
  selector:
    app: coern
