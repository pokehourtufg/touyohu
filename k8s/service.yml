apiVersion: v1
kind: Service
metadata:
  name: fewer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fewer"
    port: 8080
  selector:
    app: fewer
