apiVersion: v1
kind: Service
metadata:
  name: uyhgf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uyhgf"
    port: 8080
  selector:
    app: uyhgf
