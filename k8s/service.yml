apiVersion: v1
kind: Service
metadata:
  name: bgrty
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bgrty"
    port: 8080
  selector:
    app: bgrty
